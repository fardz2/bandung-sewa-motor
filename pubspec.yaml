name: bandung_sewa_motor
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.6
  firebase_core: ^2.32.0
  flutter:
    sdk: flutter
  flutter_layout_grid: ^2.0.7
  flutter_native_splash: ^2.4.0
  get: 4.6.6
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_lints: 4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/images/
